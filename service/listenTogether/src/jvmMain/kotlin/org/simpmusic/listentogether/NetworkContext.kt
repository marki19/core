package org.simpmusic.listentogether

/**
 * JVM: the JVM `NetworkMonitor` polls `java.net.NetworkInterface` and needs no context.
 * The argument is accepted (and ignored) to satisfy the `expect` constructor signature.
 */
actual class NetworkContext
